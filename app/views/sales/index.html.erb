<% @sales.each do |sale| %>
  <p>
    <%= link_to(sale.suburb, sale_path(sale.id)) %> 
    <%= link_to('Edit', edit_sale_path(sale.id)) %>
    <%= link_to('Delete', delete_sale_path(sale.id)) %>
  </p>
<% end %>